spring:
  application:
    name: Hyrule
  datasource:
    url: jdbc:postgresql://localhost:5432/Hyrule
    username: ##USERNAME
    password: ##PASSWORD

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect